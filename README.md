
  # Vault iOS App Prototype

  This is a code bundle for Vault iOS App Prototype. The original project is available at https://www.figma.com/design/28w3RTxcOyTNJggupSvITj/Vault-iOS-App-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  